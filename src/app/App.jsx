// Application composition entry point.
// The current finance implementation remains in ../App.jsx while the codebase is
// incrementally migrated into feature and layout modules without changing behavior.
export { default } from '../App.jsx';
